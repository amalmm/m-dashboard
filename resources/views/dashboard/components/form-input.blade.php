 <div>
    <input
type="{{$type ?? '' }}"
placeholder="{{ $placeholder ?? ''}}"
value="{{ $value ?? '' }}"
 class="w-100   border-0 border-b-1   p-2 w-full    bg-gray-100   focus:border-blue-500"
>
 </div>
